<Window x:Class="BlackJack.BustedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlackJack"
        mc:Ignorable="d"
        Title="BustedWindow" Height="600" Width="800">
    <Border Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row ="0" Text="You Went Bust" FontFamily="Trebuchet MS" FontSize="72" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button x:Name="PlayAgain" Click="PlayAgain_Click" Grid.Column="0" Content="Play Again" Margin="5" FontFamily="Trebuchet MS" FontSize="24" />
                <Button x:Name="Exit" Click="Exit_Click" Grid.Column="1" Content="Exit Game" Margin="5" FontFamily="Trebuchet MS" FontSize="24" />
            </Grid>
        </Grid>
    </Border>
</Window>
