<Window x:Class="BlackJack.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlackJack"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">


    <Border  Padding="15" Background="Green">
        <Grid x:Name="MainBorder">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="100" />
                </Grid.RowDefinitions>
                <TextBlock Text="BlackJack" FontFamily="Trebuchet MS" FontSize="128" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" />
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button x:Name="Play" Click="Play_Click" Content="Play" FontFamily="Trebuchet MS" FontSize="48" Grid.Column="0" Margin="10" />
                    <Button x:Name="Exit" Click="Exit_Click" Content="Exit" FontFamily="Trebuchet MS" FontSize="48" Grid.Column="1" Margin="10" />
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
